projection implementation in class ZBPC_RS_ACONN unique;
strict ( 2 );
use draft;
define behavior for ZC_RS_ACONN1 alias FlightConn
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}